# 汉诺塔动画展示

一个使用 **React 19** + **TailwindCSS 4** 构建的交互式汉诺塔（Tower of Hanoi）算法可视化应用。

![Hanoi Tower](https://img.shields.io/badge/React-19-blue?logo=react)
![TailwindCSS](https://img.shields.io/badge/TailwindCSS-4-38B2AC?logo=tailwindcss)
![TypeScript](https://img.shields.io/badge/TypeScript-5.6-3178C6?logo=typescript)
![License](https://img.shields.io/badge/License-MIT-green)

## ✨ 功能特性

- 🎯 **完整的汉诺塔演示** - 支持 3-7 个圆盘的动画展示
- ▶️ **灵活的播放控制** - 自动播放、暂停、上一步、下一步、重置
- 🎨 **现代极简设计** - 深蓝色主题 + 紫色渐变圆盘
- 📱 **完全响应式** - 完美适配手机、平板、桌面屏幕
- ⚡ **流畅动画** - 弹性缓动，500-800ms 的圆盘移动效果
- 📊 **实时进度** - 显示当前步数和总步数
- 📚 **教学说明** - 包含详细的算法解释和递归思想

## 🚀 快速开始

### 前置要求
- Node.js 18+
- pnpm 10+（或 npm/yarn）

### 安装依赖
```bash
pnpm install
```

### 开发模式
```bash
pnpm dev
```
访问 `http://localhost:3000` 查看应用

### 构建生产版本
```bash
pnpm build
```

### 预览生产构建
```bash
pnpm preview
```

## 📁 项目结构

```
hanoi_tower/
├── client/
│   ├── src/
│   │   ├── components/
│   │   │   ├── HanoiVisualization.tsx    # 可视化组件
│   │   │   ├── HanoiControls.tsx         # 控制面板
│   │   │   └── ui/                       # shadcn/ui 组件
│   │   ├── hooks/
│   │   │   └── useHanoiTower.ts          # 核心逻辑 hook
│   │   ├── pages/
│   │   │   ├── Home.tsx                  # 主页面
│   │   │   └── NotFound.tsx              # 404 页面
│   │   ├── App.tsx                       # 应用入口
│   │   ├── main.tsx                      # React 入口
│   │   └── index.css                     # 全局样式
│   └── index.html
├── server/                               # 占位符（静态项目）
├── shared/                               # 占位符
├── package.json
├── vite.config.ts
├── tsconfig.json
└── components.json
```

## 🔧 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 19 | UI 框架 |
| TailwindCSS | 4 | 样式框架 |
| TypeScript | 5.6 | 类型系统 |
| Vite | 7 | 构建工具 |
| Wouter | 3 | 路由管理 |
| shadcn/ui | - | UI 组件库 |
| Lucide React | - | 图标库 |
| Framer Motion | 12 | 动画库 |

## 💡 核心概念

### 汉诺塔问题
汉诺塔是一个经典的递归算法问题：
- 有三个塔（A、B、C）和 N 个大小不同的圆盘
- 初始状态：所有圆盘在塔 A 上，按大小从下到上排列
- 目标：将所有圆盘移到塔 C
- 规则：
  - 每次只能移动一个圆盘
  - 大圆盘不能放在小圆盘上方

### 递归解法
```
移动 N 个圆盘从 A 到 C：
1. 将 N-1 个圆盘从 A 移到 B（以 C 为辅助）
2. 将第 N 个圆盘从 A 移到 C
3. 将 N-1 个圆盘从 B 移到 C（以 A 为辅助）
```

### 复杂度
- 时间复杂度：O(2^N)
- 移动次数：2^N - 1
  - 3 个圆盘：7 次
  - 4 个圆盘：15 次
  - 5 个圆盘：31 次

## 🎨 设计特点

### 颜色方案
- **主色调** - 深蓝色 (#1e3a8a)
- **强调色** - 紫色 (#8b5cf6)
- **背景** - 极浅灰蓝 (#f8fafc)
- **圆盘** - 紫粉渐变

### 动画效果
- **圆盘移动** - 弹性缓动（cubic-bezier）
- **按钮交互** - 缩放 + 颜色变化
- **页面加载** - 淡入上升效果

## 📖 使用指南

### 基本操作
1. **选择圆盘数量** - 点击 3-7 的按钮
2. **播放动画** - 点击 "播放" 按钮自动演示
3. **手动控制** - 使用 "上一步" / "下一步" 逐步查看
4. **重置游戏** - 点击 "重置" 按钮回到初始状态

### 进度跟踪
- 进度条显示当前完成度
- 显示 "当前步数 / 总步数"
- 完成时显示成功提示

## 🔌 API 文档

### useHanoiTower Hook

```typescript
const hanoi = useHanoiTower(initialDiskCount);

// 属性
hanoi.rods              // 三个塔的圆盘数组
hanoi.diskCount         // 当前圆盘数量
hanoi.isPlaying         // 是否正在播放
hanoi.currentMoveIndex  // 当前步数
hanoi.totalMoves        // 总步数
hanoi.progress          // 进度百分比

// 方法
hanoi.togglePlayback()  // 播放/暂停
hanoi.nextMove()        // 下一步
hanoi.previousMove()    // 上一步
hanoi.reset()           // 重置
hanoi.changeDiskCount(n) // 改变圆盘数量
```

## 🛠️ 开发指南

### 添加新功能

#### 1. 添加音效反馈
```typescript
// 在 useHanoiTower 中添加
const playSound = () => {
  const audio = new Audio('/sounds/move.mp3');
  audio.play();
};
```

#### 2. 添加游戏统计
```typescript
// 创建新的 hook
const useGameStats = () => {
  const [stats, setStats] = useState({
    fastestTime: null,
    leastMoves: null,
    gamesPlayed: 0
  });
  // ...
};
```

#### 3. 添加难度排行榜
```typescript
// 在 Home.tsx 中添加新的组件
<Leaderboard diskCount={diskCount} />
```

### 代码规范
- 使用 TypeScript 进行类型检查
- 遵循 Prettier 代码格式
- 组件使用 React Hooks
- 样式使用 TailwindCSS 工具类

## 📦 部署

### 部署到 Vercel
```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 部署到 GitHub Pages
```bash
# 构建
pnpm build

# 推送到 gh-pages 分支
git subtree push --prefix dist origin gh-pages
```

### 部署到自己的服务器
```bash
# 构建
pnpm build

# 上传 dist 目录到服务器
scp -r dist/* user@server:/var/www/hanoi-tower
```

## 🐛 常见问题

### Q: 如何改变圆盘的颜色？
A: 编辑 `client/src/components/HanoiVisualization.tsx` 中的 `getDiskColor` 函数

### Q: 如何加快/减慢动画速度？
A: 修改 `client/src/hooks/useHanoiTower.ts` 中的 `setTimeout` 延迟时间（目前为 800ms）

### Q: 如何支持更多圆盘？
A: 在 `HanoiControls.tsx` 中的按钮数组添加更多数字

### Q: 如何添加黑暗模式？
A: 在 `App.tsx` 中启用 `switchable` ThemeProvider，并在 `index.css` 中定义 `.dark` 样式

## 📝 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

如有问题或建议，欢迎提交 Issue。

## 🙏 致谢

感谢所有使用和支持这个项目的人！

---

**⭐ 如果这个项目对你有帮助，请给个 Star！**
